# PIRATE BASH — playable

Play: https://rburnett66.github.io/pirate-bash/

This repo holds ONLY the built playable (`index.html`, `src/`, `public/`), published by GitHub Pages.
The game's source, docs and prototypes live in a separate PRIVATE repo and are republished here with
`node scripts/publish-play.cjs` from that repo.
